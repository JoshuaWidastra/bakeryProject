<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home | Bakery Shop</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet" />
    <link href="/css/styles.css" rel="stylesheet" />
    <style>
      .hero {
        position: relative;
        overflow: hidden;
        height: 100vh; /* Full viewport height */
        color: white;
      }
      .hero img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        position: absolute;
        top: 0;
        left: 0;
        z-index: 1; /* Ensures image is behind text and navbar */
      }
      .navbar {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        background: rgba(255, 255, 255, 0.3); /* Semi-transparent background */
        backdrop-filter: blur(15px); /* Increased blur effect */
        z-index: 1000; /* Ensures navbar is on top of the image */
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.2); /* Optional: adds shadow for better separation */
      }
      .navbar-brand {
        font-size: 2.5rem; /* Increase font size */
        margin-left: 2rem; /* Adjust margin to shift text to the right */
        color: peru;
      }
      .navbar-nav .nav-link {
        color: white !important; /* Set font color to white */
        transition: color 0.3s ease; /* Smooth color transition */
      }
      .navbar-nav .nav-link:hover {
        color: #f8c146; /* Change color on hover */
      }
      .hero-text {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        text-align: center;
        z-index: 1000; /* Ensures text is on top */
      }
      .btn {
        transition: background-color 0.3s ease, transform 0.3s ease; /* Smooth transition for button */
      }
      .btn:hover {
        background-color: #f8c146; /* Change background color on hover */
        transform: scale(1.05); /* Slightly enlarge the button */
      }
      .navbar-nav .nav-item .nav-link {
        color: white !important;
        transition: color 0.3s ease;
      }

      .navbar-nav .nav-item .nav-link:hover {
        color: #f8c146 !important;
      }
    </style>
  </head>
  <body>
    <header class="hero">
      <nav class="navbar navbar-expand-lg navbar-light">
        <a class="navbar-brand" href="/">Bakery Shop</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item">
              <a class="nav-link" href="/products">Products</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/cart">Cart</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/login">Login</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/register">Register</a>
            </li>
          </ul>
        </div>
      </nav>
      <img src="/images/landingPage.jpg" alt="Bakery" class="img-fluid" />
      <div class="hero-text">
        <h1 class="display-3">Welcome to Bakery Shop!</h1>
        <p class="lead">
          Discover our delicious cakes and pastries. Order now and enjoy!
        </p>
      </div>
    </header>

    <script src="/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
